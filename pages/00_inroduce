import streamlit as st

# ---------------------------
# 기본 설정
# ---------------------------
st.set_page_config(
    page_title="😎 나를 소개합니다",
    page_icon="🔥",
    layout="centered"
)

# ---------------------------
# 배경 CSS (몽글 + 컬러)
# ---------------------------
st.markdown(
    """
    <style>
    .stApp {
        background: linear-gradient(180deg, #FFF6E5, #E8F6FF);
    }
    h1, h2, h3 {
        text-align: center;
    }
    </style>
    """,
    unsafe_allow_html=True
)

# ---------------------------
# 헤더
# ---------------------------
st.markdown("# 👋 안녕하세요!")
st.markdown("## 😎 저는 이런 사람입니다")

st.image(
    "https://i.imgflip.com/4/30b1gx.jpg",
    caption="처음 코딩할 때의 나 😅",
    use_container_width=True
)

st.divider()

# ---------------------------
# 자기소개
# ---------------------------
st.markdown(
    """
    ### 🧑‍💻 한 줄 소개  
    👉 **코딩은 어렵지만… 재미는 있다!**  

    ### 💡 이런 걸 좋아해요
    - 🤖 AI, 자동화  
    - 📈 투자 이야기 (가끔은 눈물 😭)  
    - 👶 우리 아기 사진 모으기  
    - ☕ 커피 마시며 멍 때리기  

    ### 😅 이런 단점도 있어요
    - 버그 나면 현실 도피
    - "이건 금방 되겠지" → 3시간 경과
    """
)

st.image(
    "https://i.imgflip.com/1bij.jpg",
    caption="버그 발견 직후 내 표정",
    use_container_width=True
)

st.divider()

# ---------------------------
# 능력치 (재미용)
# ---------------------------
st.markdown("## 📊 나의 능력치 (주관적임 주의 ⚠️)")

st.progress(70)
st.caption("🐍 파이썬 친밀도 70%")

st.progress(40)
st.caption("🧠 알고리즘 이해도 40%")

st.progress(90)
st.caption("🔥 의욕 90% (지속시간은 랜덤)")

st.image(
    "https://i.imgflip.com/26am.jpg",
    caption="의욕은 항상 만렙",
    use_container_width=True
)

st.divider()

# ---------------------------
# 버튼 인터랙션
# ---------------------------
st.markdown("## 🎁 버튼을 눌러보세요")

if st.button("🚀 나의 각오 보기"):
    st.success("언젠가는… 나도 고수 개발자가 된다 😎🔥")

if st.button("😂 오늘의 명언"):
    st.info("코드는 거짓말을 안 한다. 하지만 개발자는 가끔 한다.")

st.divider()

# ---------------------------
# 푸터
# ---------------------------
st.markdown(
    """
    ### 🙌 방문해줘서 감사합니다!
    다음엔 **더 웃긴 버전**으로 돌아올게요 😆  
    """
)
